# 标印 LabelPress（便携版）

免安装、免配置的商品信息管理 + 条形码生成 + 标签打印工具。

## 一、怎么用（给电脑小白）

1. **把 `标印LabelPress-便携版-1.2.0.exe` 放到任何文件夹**（比如 U 盘、桌面、D 盘）
2. **双击它**，等 2~5 秒（首次启动稍慢），软件窗口会自动打开
3. 用完直接点右上角 ✕ 关闭即可

> ⚠️ 如果 Windows 提示"未知发布者/是否允许运行"，点「更多信息」→「仍要运行」即可（软件未做商业签名，属正常提示）。

## 二、主要功能

- **新增/编辑商品**：点「＋ 新增商品」，填写名称、SKU、分类、单位、价格、规格、备注；**表单内按 Enter 直接保存**，新增商品保存后自动清空并聚焦名称框，方便连续录入
- **快速打印**：勾选商品后点「⚡ 快速打印」按当前预设和上次的打印机直接输出（不打开设计器）；也可把鼠标悬停在商品行上，点行尾的 🖨 直打单个商品
- **行内改价**：列表里**双击价格**直接修改（Enter 或点别处保存，Esc 取消）
- **列表筛选与排序**：顶部搜索框按名称 / SKU / 条码查找；「全部分类」下拉按分类筛选；排序下拉支持名称 A→Z、价格低→高/高→低、最近修改
- **打印历史 / 一键重打**：点「🕘 打印历史」查看最近 20 次打印记录（快速打印也会记入），点「重打」按当时的内容与设置原样输出
- **条形码**：选择类型（EAN-13 最常用，还有 Code128 / UPC / Code39 / ITF14），输入条码内容（**留空自动使用 SKU**）；右侧实时预览，绿色"✓ 条码有效"表示可用；条码以**矢量（SVG）**生成，打印任意尺寸都清晰
- **打印标签（可视化设计器）**：
  - 勾选列表左侧方框 → 点「打印选中」；或直接「打印全部」
  - 打开**打印标签设计器**：左侧可设置每个元素（商品名称 / 价格 / SKU / 条形码）的**位置、宽度、字号、对齐、加粗、显示/隐藏**；右侧**所见即所得实时预览**
  - **在预览上直接拖动元素**可调整位置（选中元素显示虚线框）；拖动时靠近其它元素的边缘或标签边/中心线会自动吸附对齐，并显示红色参考虚线；选中后用**方向键**精确微调（0.5mm/步，按住 Shift 为 2mm）
  - **多选与批量对齐**：Ctrl+点选多个元素，或在画布空白处**拖框选**；选中 ≥2 个时预览上方出现**对齐工具栏**（左/水平居中/右/顶/垂直居中/底对齐 + 水平/垂直等距分布）；Ctrl+A 全选、Esc 取消多选；多选后可直接拖动整体平移
  - **撤销 / 重做**：点设计器顶部的「↶ 撤销」「↷ 重做」按钮（或 Ctrl+Z / Ctrl+Y），拖动、删除、改参数等操作都可撤销
  - **自定义文本元素**：点「＋ 添加自定义文本」可添加任意文本（如生产日期、保质期、备注）；元素名称**自动取文本前 5 个字**，也可在「元素名称」框手动改名（改过后名称不再随文本变化）；支持改内容/字号/加粗/颜色、**⧉ 复制**（向下复制一份相同元素，可连点多份）、Ctrl+C / Ctrl+V 复制粘贴（支持多个）、Delete 删除
  - **长名称不怕截断**：文本超出宽度时**自动缩小字号**（最小 1.2mm），打印与预览一致
  - 顶部可选标签尺寸：60×40mm、50×30mm、40×30mm、40×25mm、30×40mm（竖版）、80×100mm、100×80mm（热敏/标签打印机），或 A4 整页（普通打印机）；**A4 整页预览显示真实切线网格**（纵向 3×6、横向 4×4 个标签）
  - **打印预设**：左侧「打印预设」区可把调好的布局**保存为预设**（如"热敏 60×40"、"A4 商用"），下次打印从下拉直接选择即可；**打印机、方向、份数也随预设一起记忆**——切换预设自动换到对应的打印机和方向；支持「＋ 新建」多套预设、「⬜ 新建空白」建一个**全部元素取消勾选**的空白预设从零排版、「🗑 删除」不需要的预设
  - **打印选项**：默认**直接打印**（跳过系统打印对话框）；如需每次手动确认，取消勾选「直接打印」即可；**切换横/纵向时预览实时变化**（热敏标签旋转 90°、A4 纸张横放并重排网格）
  - **布局改动自动保存**：调完版面即使直接关闭设计器（不打印），改动也会自动保存到当前预设，不会丢失
  - **元素旋转与竖排**：每个元素卡片新增「旋转」下拉（0°/90°/180°/270°）和「排列」下拉（横排/竖排）——竖排让每个字直立、从上往下写（如「农夫山泉」竖着贴在瓶子上），旋转则是整行侧躺；预览与打印一致
  - **字体可选**：文本元素的「字体」下拉可选黑体、宋体、楷体、隶书等系统字体，随预设记忆
  - **日期占位符**：自定义文本里写 `{今天}` `{明天}` `{昨天}`，打印时自动替换为当天日期，生产日期/保质期不用天天手改
  - **打印偏移补偿**：打印选项里可设「偏移X / 偏移Y」（毫米），校正热敏打印机打歪/偏移的问题（内容偏左就填正的偏移X）
  - **预览缩放**：预览区按住 **Ctrl + 滚轮**可 0.4~4 倍手动缩放，切换尺寸/方向恢复自适应
  - **导出 PDF**：设计器顶部「📄 导出PDF」可把当前版式（含 A4 整页排布）存成 PDF 文件，不走打印机
  - 点「打印」后按当前设置输出，成功会提示「已发送到某打印机 × N 份」，并自动记入打印历史
  - 预览区右上角可用「‹ x/N ›」**切换样板商品**，检查不同长度名称的排版效果
- **搜索**：顶部搜索框可按名称 / SKU / 条码查找

## 三、数据安全（重要）

- 所有商品数据保存在 **exe 同级的「数据」文件夹**里（products.json）
- **换电脑 / 换设备**：把 exe 和「数据」文件夹**一起**复制走即可，数据随身带
- **备份**：随时点「备份数据」把数据另存为一份文件；「恢复数据」可从备份文件还原
- 每次保存前软件会自动保留上一份数据（products.backup.json）；**每天首次保存还会自动快照到「数据/backups/」，保留最近 7 天**，误删可从这里找回

## 四、常见问题

| 问题 | 解决 |
|---|---|
| 双击没反应 | 确认是 64 位 Windows；等 5 秒；查看杀毒软件是否拦截 |
| 提示未知发布者 | 点「更多信息」→「仍要运行」（首次） |
| 换电脑后数据没了 | 必须连「数据」文件夹一起复制，不能只拷 exe |
| 打印尺寸不对 | 在打印设置里选择与标签纸匹配的尺寸；热敏打印机请先在系统里把该打印机设为默认 |
| 打印内容偏移/压边 | 打印选项里调「偏移X / 偏移Y」补偿（内容偏左填正的偏移X，偏上填正的偏移Y） |

## 五、开发信息

- 技术栈：Electron + JsBarcode，数据为 JSON 文件（原子写入 + 自动备份 + 每日快照轮换）
- 源码在项目目录：`main.js`（主进程/数据/打印/PDF 导出）、`renderer/`（界面 + 标签渲染 `label-render.js`）
- 重新打包：`npm install` 后执行 `npm run dist`
