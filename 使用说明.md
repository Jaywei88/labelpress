# 商品条码管理（便携版）

免安装、免配置的商品信息管理 + 条形码生成 + 标签打印工具。

## 一、怎么用（给电脑小白）

1. **把 `商品条码管理-便携版-1.0.0.exe` 放到任何文件夹**（比如 U 盘、桌面、D 盘）
2. **双击它**，等 2~5 秒（首次启动稍慢），软件窗口会自动打开
3. 用完直接点右上角 ✕ 关闭即可

> ⚠️ 如果 Windows 提示"未知发布者/是否允许运行"，点「更多信息」→「仍要运行」即可（软件未做商业签名，属正常提示）。

## 二、主要功能

- **新增/编辑商品**：点「＋ 新增商品」，填写名称、SKU、分类、单位、价格、规格、备注
- **条形码**：选择类型（EAN-13 最常用，还有 Code128 / UPC / Code39 / ITF14），输入条码内容（**留空自动使用 SKU**）；右侧实时预览，绿色"✓ 条码有效"表示可用
- **打印标签（可视化设计器）**：
  - 勾选列表左侧方框 → 点「打印选中」；或直接「打印全部」
  - 打开**打印标签设计器**：左侧可设置每个元素（商品名称 / 价格 / SKU / 条形码）的**位置、宽度、字号、对齐、加粗、显示/隐藏**；右侧**所见即所得实时预览**
  - **在预览上直接拖动元素**可调整位置（选中元素显示虚线框）；**拖动时靠近其它元素的边缘会自动吸附对齐，并显示红色参考虚线**帮助对齐；选中后用**方向键**可精确微调（0.5mm/步，按住 Shift 为 2mm）
  - **自定义文本元素**：点「＋ 添加自定义文本」可添加任意文本（如生产日期、保质期、备注）；支持改内容/字号/加粗/颜色、**⧉ 复制**（向下复制一份相同元素，可连点多份）、Ctrl+C / Ctrl+V 复制粘贴、Delete 删除
  - 顶部可选标签尺寸：60×40mm、50×30mm、40×30mm、40×25mm、30×40mm（竖版）、80×100mm、100×80mm（热敏/标签打印机），或 A4 整页（普通打印机）
  - **打印预设**：左侧「打印预设」区可把调好的布局**保存为预设**（如"热敏 60×40"、"A4 商用"），下次打印从下拉直接选择即可，不用重新调整；也支持「＋ 新建」多套预设、「🗑 删除」不需要的预设
  - **打印选项**：可记忆**打印机、方向（横/纵向）、份数**，默认**直接打印**（跳过系统打印对话框，用上次的打印机和设置直接输出）；如需每次手动确认，取消勾选「直接打印」即可；**切换横/纵向时，右侧预览会跟着旋转**，可直观看到打印出来的方向
  - 点「打印」后按当前设置输出；**布局设置会自动回存到当前预设**，下次打开沿用
- **搜索**：顶部搜索框可按名称 / SKU / 条码查找

## 三、数据安全（重要）

- 所有商品数据保存在 **exe 同级的「数据」文件夹**里（products.json）
- **换电脑 / 换设备**：把 exe 和「数据」文件夹**一起**复制走即可，数据随身带
- **备份**：随时点「备份数据」把数据另存为一份文件；「恢复数据」可从备份文件还原
- 每次保存前软件会自动保留上一份数据（products.backup.json），以防误操作

## 四、常见问题

| 问题 | 解决 |
|---|---|
| 双击没反应 | 确认是 64 位 Windows；等 5 秒；查看杀毒软件是否拦截 |
| 提示未知发布者 | 点「更多信息」→「仍要运行」（首次） |
| 换电脑后数据没了 | 必须连「数据」文件夹一起复制，不能只拷 exe |
| 打印尺寸不对 | 在打印设置里选择与标签纸匹配的尺寸；热敏打印机请先在系统里把该打印机设为默认 |

## 五、开发信息

- 技术栈：Electron + JsBarcode，数据为 JSON 文件（原子写入 + 自动备份）
- 源码在项目目录：`main.js`（主进程/数据/打印）、`renderer/`（界面 + 标签渲染 `label-render.js`）
- 重新打包：`npm install` 后执行 `npm run dist`
