# 修改文档：打印记忆 + 自定义元素（v1.1 计划）

> 本文档记录 2026-09 对「商品条码管理」的一次功能升级：① 打印时记住上次使用的打印机与打印设置，不再每次被系统对话框重置；② 打印设计器支持自定义元素（自由添加/移动/向下复制多份）；③ dlabel 式交互梳理与后续可优化点清单。

- 涉及文件：`main.js`、`preload.js`、`renderer/index.html`、`renderer/app.js`、`renderer/label-render.js`、`renderer/style.css`
- 验证：`scripts/test-advanced.js`（自定义元素 + 打印选项）、`scripts/test-presets.js`（预设回归）、`scripts/test-legacy-presets.js`（旧格式迁移）、内置冒烟测试，全部通过

---

## 一、功能 1：记住上次打印机与打印设置

### 背景问题
之前点「打印」后弹出的是**系统打印对话框**，每次都会重置到系统默认打印机、默认方向等，用户每次都要重新选打印机、调横纵向，非常繁琐。

### 实现方案
借鉴常见标签软件的做法——**默认不再弹系统对话框，直接发送到上次使用的打印机**，所有设置由应用自己记住：

1. **主进程（main.js）**
   - 新增 `print-options.json` 持久化文件（存于「数据」目录，随数据便携迁移）
   - 新增 IPC：`print:getPrinters`（枚举系统打印机）、`print:getPrintOptions` / `print:savePrintOptions`（读写记忆的选项）
   - `printLabels` 增加 `printOptions` 参数透传：`deviceName`（打印机）、`landscape`（横/纵向）、`copies`（份数）、`silent`（是否弹系统对话框）

2. **渲染层（app.js / index.html）**
   - 打印设计器左侧新增「打印选项」卡片：
     - **打印机**下拉：列出全部系统打印机 + 「系统默认打印机」；自动选中上次使用的那台（若已不存在则回退系统默认，兼容换设备）
     - **方向**：纵向 / 横向，记住上次选择
     - **份数**：数字输入，记住上次份数
     - **直接打印**复选框：默认勾选（跳过系统对话框、用记忆的设置直接打印）；取消勾选则弹系统对话框手动确认
   - 选项**变更即自动保存**，打印时也会再同步保存一次

### 效果
- 换打印机后，下次打开打印界面自动记住；方向、份数同样保持
- 无需每次重新设置

---

## 二、功能 2：自定义元素

### 背景问题
原设计器只有「商品名称 / 价格 / SKU / 条形码」四个固定元素，用户无法表达"生产日期""保质期""存储条件"等额外信息，版面灵活性差。

### 实现方案
1. **数据结构（label-render.js）**
   - 设置对象新增 `custom: []` 数组，每个元素为自由文本：
     ```js
     { id, type: 'text', text, x, y, w, fontSize, align, bold, color, visible }
     ```
   - `normalizeSettings` 保留并规范化 `custom`；`renderLabelHTML` / `buildPrintHtml` 渲染自定义元素（打印与预览同一套逻辑，所见即所得）
   - 自动生成唯一 `id`，复制时强制换新 id，避免冲突

2. **交互（app.js / index.html / style.css）**
   - 元素列表底部新增「**＋ 添加自定义文本**」按钮，点击即在标签上添加一个可编辑文本元素
   - 自定义元素卡片支持：
     - **文本内容**直接编辑（如"生产日期：2026-01-01"）
     - **位置 X/Y、宽度、字号、对齐（左/中/右）、加粗、颜色**，与固定元素一致的参数面板
     - **显示/隐藏**开关
     - 「**⧉ 复制**」按钮：**向下复制一个相同元素**（新元素 Y 偏移 +6mm，不重叠），可连续点击复制多份
     - 「**🗑**」删除按钮（有确认）
   - 预览区**拖动**、**点击选中**自定义元素（虚线框），与固定元素完全一致
   - **键盘交互**（参考 dlabel）：
     - 方向键：微调选中元素位置（0.5mm/步，按住 Shift 为 2mm/步）
     - `Ctrl+C` / `Ctrl+V`：复制/粘贴自定义元素（粘贴副本向下偏移 6mm）
     - `Delete`：删除选中的自定义元素
   - 切换标签尺寸、恢复默认布局时**保留**已添加的自定义元素
   - 自定义元素随预设一起保存/切换

### 效果
- 任何额外信息都能加：生产日期、保质期、条形码下面的备注、公司名、警告语……
- 一个标签上可放多个相同/不同文本，位置随心拖动，快速排布

---

## 三、参考 dlabel 的交互优化点（本次已做 / 后续可做）

### 已实现的 dlabel 式交互
| 交互 | 说明 |
|---|---|
| 元素自由拖动 | 预览区直接拖，mm 级精确定位 |
| 对齐参考线 + 吸附 | 拖动时若与其它元素的左/中/右、上/中/下边缘接近（≤0.5mm），自动吸附并对齐显示红色虚线参考线 |
| 方向实时预览 | 打印选项切横向/纵向时，预览标签整体旋转 90°，与实际打印方向一致 |
| 向下复制多份 | 卡片「复制」按钮 / Ctrl+C+V，连续复制 |
| 键盘微调 | 方向键 0.5mm，Shift 加速 |
| 元素多属性面板 | 位置/尺寸/字号/对齐/加粗/颜色/显隐 |
| 设置记忆 | 打印机/方向/份数/预设全部记忆 |
| 所见即所得 | 预览与打印同源渲染 |

### 后续还能优化的点（建议清单）
1. **撤销/重做**（Ctrl+Z / Ctrl+Y）：所有布局操作的撤销栈，是设计类软件最核心的缺失
2. **多选元素**：框选 / Ctrl 点选多个，批量移动、对齐、统一字号颜色
3. **对齐与分布工具**：左对齐/居中/右对齐、垂直水平居中、等间距分布、靠边吸附
4. **参考线 / 磁吸**：拖动时对齐到标签边缘或元素边缘
5. **缩放与画布**：Ctrl+滚轮缩放预览、1:1 实际尺寸查看（对 30×40mm 这类小标签很重要）
6. **图片 / 二维码 / 形状元素**：Logo、商品图、QR 码、线条/矩形边框
7. **元素旋转**：竖排文本、倾斜装饰
8. **图层管理**：置顶/置底、上下移一层
9. **变量/合并打印**：自定义元素支持占位符（如 `{name}`、`{日期}`）按商品批量填充
10. **打印前检查**：元素越界红色警告、字号过小提示
11. **打印边距/偏移补偿**：部分热敏打印机有固定偏移，提供打印偏移校正
12. **模板分享/克隆**：预设导出导入为文件，换电脑直接复用
13. **记住窗口位置与上次打印标签尺寸**直达入口

---

## 四、测试记录

| 测试 | 覆盖 | 结果 |
|---|---|---|
| `scripts/test-advanced.js` | 打印选项存取+默认静默、自定义元素添加/渲染/复制(偏移6mm)/删除、预设持久化自定义元素、选项变更自动保存 | ✅ 通过 |
| `scripts/test-presets.js` | 预设创建/切换/删除回归 | ✅ 通过 |
| `scripts/test-legacy-presets.js` | 旧版单份设置自动迁移 | ✅ 通过 |
| 内置冒烟测试 | 应用启动/增删查/条码 | ✅ SMOKE_RESULT=OK |

> 打包：用户确认本次暂不打包，待功能稳定后再执行 `npm run dist`。

---

# 修改文档 v1.2：编辑器体验大版本（撤销 / 多选对齐 / A4 横向 / 自动保存）

> 2026-09-06 第二轮升级，把「后续还能优化的点」清单里的高优先级项全部落地。吸附策略参考 dlabel（对齐主打多选+工具栏，吸附只做轻量辅助）与 Figma（阈值跟屏幕像素、宁小勿大）后重新设计。

- 涉及文件：`renderer/app.js`、`renderer/label-render.js`、`renderer/index.html`、`renderer/style.css`、`main.js`、新增 `scripts/test-editor-v2.js`
- 验证：`test-editor-v2.js`（10 项新功能）+ `test-snap-orient.js` / `test-advanced.js` / `test-presets.js` / `test-legacy-presets.js` / 冒烟，全部通过

## 一、数据安全类（高优先级）

1. **关闭设计器不再丢改动**：所有改动点防抖 600ms 自动写回当前预设（`scheduleAutoSave`/`flushAutoSave`）；点「取消」关闭时立即落盘。切换预设前也会先把当前调整写回原预设。
2. **撤销 / 重做**：Ctrl+Z / Ctrl+Y（含 Ctrl+Shift+Z），栈深 50 步。覆盖拖动（松手记一步）、方向键微调（700ms 内连续按合并为一步）、属性输入（聚焦后整段编辑算一步）、显示/隐藏、增/删/复制自定义元素、切尺寸、恢复默认、对齐/分布、粘贴。切换预设/打开设计器时清栈。
3. **元素越界修复**：拖拽/微调/对齐结果全部 clamp 到标签边界内（此前元素可被拖出右/下边界后"看不见"）。

## 二、打印正确性（高优先级）

4. **A4 横向适配**：`buildPrintHtml` 方向感知——横向 `@page size: A4 landscape` + 网格重排 4 列（纵向仍 3 列，60mm 标签 + 5mm 间距在 297/210mm 内的最优排布）。预览同步重做：A4 显示**整张纸 + 标签切线网格**（纵向 3×6、横向 4×4），第一个标签可编辑拖动，其余为静态克隆；横向时纸张横放、内容不旋转（热敏标签仍保持"整体旋转 90°"逻辑）。
5. **文本溢出自动缩字**：预览与打印同一规则——宽度不够时按 0.2mm 步进缩小字号（下限 1.2mm），不再静默裁剪；预览端 `.el` 补齐了与打印端一致的 `nowrap + overflow hidden + 1.15 行高`（顺带修复了预览换行/打印裁剪不一致的所见即所得偏差）。
6. **吸附用实测高度**：文本元素高度不再按字号估算，改用预览 DOM 实测（缩字后也准确）。

## 三、体验类

7. **打印选项绑定预设**：打印机/方向/份数/直接打印存入 `settings.printOptions` 随预设走；旧预设无此字段回退全局记忆（兼容）。切换预设选项联动，方向变化联动预览。全局 `print-options.json` 仍同步写入作为回退。
8. **打印反馈**：成功 toast「已发送到「XX 打印机」× N 份」；用户取消给轻提示；失败/超时报错误。
9. **吸附升级（克制版）**：阈值改屏幕像素制（6px ÷ 缩放，夹在 [0.3, 1.5]mm，各尺寸手感一致）；新增标签本体参照仅 5 个——左边缘/水平中心线/顶边/垂直中心线/底边；被拖元素右边缘仍不吸附；**多选拖动完全关闭吸附**。
10. **窗口 resize 自适应**：防抖 200ms 重算预览缩放。
11. **顺手修复**：首次打开设计器预览缩放掉到 0.4 倍的老问题（弹窗隐藏时读不到面板尺寸）——现在先显示弹窗再渲染预览。

## 四、锦上添花

12. **多选**：Ctrl/⌘+点选切换、画布空白拖框选（横向旋转做了屏幕→标签坐标反变换）、Ctrl+A 全选可见元素、Esc 收回单选；多选后拖动整体平移。
13. **对齐工具栏**：预览区顶部，≥2 个选中时出现——左/水平居中/右/顶/垂直居中/底对齐 + 水平/垂直等距分布（≥3 个）。
14. **批量复制/删除**：Ctrl+C/V 支持多个自定义元素；Delete 批量删除（固定元素仍只能隐藏，有提示）。
15. **预览样板切换**：预览区「‹ x/N ›」切换样板商品，验证不同长度名称的排版。
16. **拖拽性能**：拖动中只更新被拖（或多选）节点 left/top + 参考线，松手才全量重绘重测高度。

## 五、测试记录（v1.2）

| 测试 | 覆盖 | 结果 |
|---|---|---|
| `scripts/test-editor-v2.js`（新增） | 样板切换、缩字（3.5→2.47mm）、边界 clamp、撤销/重做、多选对齐+撤销恢复、打印选项随预设、关闭自动保存、重开恢复、A4 横向打印 HTML（4 列 + landscape @page）、A4 预览网格（18/16 格、第一格可编辑、横向不旋转） | ✅ 10 项全过 |
| `test-snap-orient.js` | 吸附 + 方向预览（name 宽度改为 20mm 以适配新的边界 clamp） | ✅ 通过 |
| `test-advanced.js` / `test-presets.js` / `test-legacy-presets.js` / 冒烟 | 全部回归 | ✅ 通过 |

## 六、后续还能优化的点（更新）

原 13 条清单状态：~~1 撤销重做~~、~~2 多选~~、~~3 对齐分布~~、~~4 参考线磁吸~~、~~10 打印前检查（部分：越界已 clamp + 缩字）~~、~~13 记住窗口尺寸（部分：resize 自适应已做）~~ 已完成。

仍可做的：
1. 图片 / 二维码 / 形状元素（`custom` 的 `type` 字段已留好扩展位）
2. 元素旋转（竖排文本）
3. 图层管理（置顶/置底；多选列表化）
4. 变量/合并打印（占位符如 `{name}`、`{日期}` 按商品批量填充）
5. 打印偏移补偿（热敏打印机固定偏移校正）
6. 预设导出/导入（换电脑复用）
7. Ctrl+滚轮手动缩放预览、1:1 实际尺寸查看

---

# 修改文档 v1.3：撤销按钮 / 自定义元素命名 / 空白预设

> 2026-09-06 第三轮小迭代，来自用户试用反馈的三点改进。

## 一、撤销/重做独立按钮

- 设计器头部新增「↶ 撤销」「↷ 重做」按钮（在「恢复默认」左侧），与 Ctrl+Z / Ctrl+Y 完全等效
- 无步可退/无步可进时按钮**自动置灰**（状态随每次预览重绘刷新）

## 二、自定义元素名称（自动派生 + 手动锁定）

- 数据结构：自定义元素新增 `name`（显示名称）与 `nameLocked`（是否手动改过名）字段，随预设持久化，旧数据自动兼容
- **自动派生**：未手动改名时，名称自动取文本内容**前 5 个字**（不足 5 字不补，超过截为 5 字）；输入文本时卡片标题实时跟随
- **手动改名**：卡片新增「元素名称」输入框（占位提示"文本前5字"，最长 20 字）；一旦手动修改（`nameLocked = true`），此后改文本**不再覆盖**名称
- 修复连带问题：`syncElementInputs` 字段顺序过滤排除 `name`，避免数字输入框错位

## 三、新建空白预设

- 预设区新增「⬜ 新建空白」按钮（与 保存/＋新建/🗑删除 同排，2×2 布局）
- 行为：输入名称后创建并切换到该预设——**沿用当前位置参数，但所有元素（商品名称/价格/SKU/条形码 + 全部自定义元素）自动取消勾选**，从零开始勾选排版
- 当前预设若有未保存调整，先自动写回再切换，不丢改动

## 四、测试记录（v1.3）

| 测试 | 覆盖 | 结果 |
|---|---|---|
| `test-editor-v2.js` 扩展至 13 节 | 新增 K：撤销/重做按钮禁用态与点击生效；L：名称自动派生（前 5 字）、卡片标题实时更新、手动改名锁定后不随文本变化；M：空白预设全部元素取消勾选、自动切换与持久化、删除后回退「默认」 | ✅ 全过 |
| 其余 4 套件 + 冒烟 | 全部回归 | ✅ 通过 |