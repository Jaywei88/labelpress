# 修改文档：打印记忆 + 自定义元素（v1.1 计划）

> 本文档记录 2026-09 对「商品条码管理」的一次功能升级：① 打印时记住上次使用的打印机与打印设置，不再每次被系统对话框重置；② 打印设计器支持自定义元素（自由添加/移动/向下复制多份）；③ dlabel 式交互梳理与后续可优化点清单。

- 涉及文件：`main.js`、`preload.js`、`renderer/index.html`、`renderer/app.js`、`renderer/label-render.js`、`renderer/style.css`
- 验证：`scripts/test-advanced.js`（自定义元素 + 打印选项）、`scripts/test-presets.js`（预设回归）、`scripts/test-legacy-presets.js`（旧格式迁移）、内置冒烟测试，全部通过

---

## 一、功能 1：记住上次打印机与打印设置

### 背景问题
之前点「打印」后弹出的是**系统打印对话框**，每次都会重置到系统默认打印机、默认方向等，用户每次都要重新选打印机、调横纵向，非常繁琐。

### 实现方案
借鉴常见标签软件的做法——**默认不再弹系统对话框，直接发送到上次使用的打印机**，所有设置由应用自己记住：

1. **主进程（main.js）**
   - 新增 `print-options.json` 持久化文件（存于「数据」目录，随数据便携迁移）
   - 新增 IPC：`print:getPrinters`（枚举系统打印机）、`print:getPrintOptions` / `print:savePrintOptions`（读写记忆的选项）
   - `printLabels` 增加 `printOptions` 参数透传：`deviceName`（打印机）、`landscape`（横/纵向）、`copies`（份数）、`silent`（是否弹系统对话框）

2. **渲染层（app.js / index.html）**
   - 打印设计器左侧新增「打印选项」卡片：
     - **打印机**下拉：列出全部系统打印机 + 「系统默认打印机」；自动选中上次使用的那台（若已不存在则回退系统默认，兼容换设备）
     - **方向**：纵向 / 横向，记住上次选择
     - **份数**：数字输入，记住上次份数
     - **直接打印**复选框：默认勾选（跳过系统对话框、用记忆的设置直接打印）；取消勾选则弹系统对话框手动确认
   - 选项**变更即自动保存**，打印时也会再同步保存一次

### 效果
- 换打印机后，下次打开打印界面自动记住；方向、份数同样保持
- 无需每次重新设置

---

## 二、功能 2：自定义元素

### 背景问题
原设计器只有「商品名称 / 价格 / SKU / 条形码」四个固定元素，用户无法表达"生产日期""保质期""存储条件"等额外信息，版面灵活性差。

### 实现方案
1. **数据结构（label-render.js）**
   - 设置对象新增 `custom: []` 数组，每个元素为自由文本：
     ```js
     { id, type: 'text', text, x, y, w, fontSize, align, bold, color, visible }
     ```
   - `normalizeSettings` 保留并规范化 `custom`；`renderLabelHTML` / `buildPrintHtml` 渲染自定义元素（打印与预览同一套逻辑，所见即所得）
   - 自动生成唯一 `id`，复制时强制换新 id，避免冲突

2. **交互（app.js / index.html / style.css）**
   - 元素列表底部新增「**＋ 添加自定义文本**」按钮，点击即在标签上添加一个可编辑文本元素
   - 自定义元素卡片支持：
     - **文本内容**直接编辑（如"生产日期：2026-01-01"）
     - **位置 X/Y、宽度、字号、对齐（左/中/右）、加粗、颜色**，与固定元素一致的参数面板
     - **显示/隐藏**开关
     - 「**⧉ 复制**」按钮：**向下复制一个相同元素**（新元素 Y 偏移 +6mm，不重叠），可连续点击复制多份
     - 「**🗑**」删除按钮（有确认）
   - 预览区**拖动**、**点击选中**自定义元素（虚线框），与固定元素完全一致
   - **键盘交互**（参考 dlabel）：
     - 方向键：微调选中元素位置（0.5mm/步，按住 Shift 为 2mm/步）
     - `Ctrl+C` / `Ctrl+V`：复制/粘贴自定义元素（粘贴副本向下偏移 6mm）
     - `Delete`：删除选中的自定义元素
   - 切换标签尺寸、恢复默认布局时**保留**已添加的自定义元素
   - 自定义元素随预设一起保存/切换

### 效果
- 任何额外信息都能加：生产日期、保质期、条形码下面的备注、公司名、警告语……
- 一个标签上可放多个相同/不同文本，位置随心拖动，快速排布

---

## 三、参考 dlabel 的交互优化点（本次已做 / 后续可做）

### 已实现的 dlabel 式交互
| 交互 | 说明 |
|---|---|
| 元素自由拖动 | 预览区直接拖，mm 级精确定位 |
| 对齐参考线 + 吸附 | 拖动时若与其它元素的左/中/右、上/中/下边缘接近（≤0.5mm），自动吸附并对齐显示红色虚线参考线 |
| 方向实时预览 | 打印选项切横向/纵向时，预览标签整体旋转 90°，与实际打印方向一致 |
| 向下复制多份 | 卡片「复制」按钮 / Ctrl+C+V，连续复制 |
| 键盘微调 | 方向键 0.5mm，Shift 加速 |
| 元素多属性面板 | 位置/尺寸/字号/对齐/加粗/颜色/显隐 |
| 设置记忆 | 打印机/方向/份数/预设全部记忆 |
| 所见即所得 | 预览与打印同源渲染 |

### 后续还能优化的点（建议清单）
1. **撤销/重做**（Ctrl+Z / Ctrl+Y）：所有布局操作的撤销栈，是设计类软件最核心的缺失
2. **多选元素**：框选 / Ctrl 点选多个，批量移动、对齐、统一字号颜色
3. **对齐与分布工具**：左对齐/居中/右对齐、垂直水平居中、等间距分布、靠边吸附
4. **参考线 / 磁吸**：拖动时对齐到标签边缘或元素边缘
5. **缩放与画布**：Ctrl+滚轮缩放预览、1:1 实际尺寸查看（对 30×40mm 这类小标签很重要）
6. **图片 / 二维码 / 形状元素**：Logo、商品图、QR 码、线条/矩形边框
7. **元素旋转**：竖排文本、倾斜装饰
8. **图层管理**：置顶/置底、上下移一层
9. **变量/合并打印**：自定义元素支持占位符（如 `{name}`、`{日期}`）按商品批量填充
10. **打印前检查**：元素越界红色警告、字号过小提示
11. **打印边距/偏移补偿**：部分热敏打印机有固定偏移，提供打印偏移校正
12. **模板分享/克隆**：预设导出导入为文件，换电脑直接复用
13. **记住窗口位置与上次打印标签尺寸**直达入口

---

## 四、测试记录

| 测试 | 覆盖 | 结果 |
|---|---|---|
| `scripts/test-advanced.js` | 打印选项存取+默认静默、自定义元素添加/渲染/复制(偏移6mm)/删除、预设持久化自定义元素、选项变更自动保存 | ✅ 通过 |
| `scripts/test-presets.js` | 预设创建/切换/删除回归 | ✅ 通过 |
| `scripts/test-legacy-presets.js` | 旧版单份设置自动迁移 | ✅ 通过 |
| 内置冒烟测试 | 应用启动/增删查/条码 | ✅ SMOKE_RESULT=OK |

> 打包：用户确认本次暂不打包，待功能稳定后再执行 `npm run dist`。